# System packages required for Streamlit.io deployment
# This file is read by Streamlit.io to install system dependencies
# Each line should contain one package name

# Currently no system packages are required for this application
# The application only uses Python packages listed in requirements.txt

# If you need system packages in the future, uncomment and add them here:
# Examples:
# build-essential
# gcc
# python3-dev
# libffi-dev
# libssl-dev